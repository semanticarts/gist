### Major Updates

- Set ranges of `gist:containedText`, `gist:uniqueText`, `gist:encryptedText`, `gist:description`, `gist:name` and `gist:symbol` to `rdf:PlainLiteral`, which encompasses string literals with or without language tags. Changed corresponding restrictions on `gist:Text` and `gist:ID` from `xsd:string` to `rdf:PlainLiteral`. Issue [#1302](https://github.com/semanticarts/gist/issues/1302).


